controllers:
    resource:
        path: ../src/Controller/
        namespace: App\Controller
    type: attribute

# config/routes.yaml
index:
    path: /
    controller: App\Controller\ProduitController::index # Remplacez HomeController par votre contrôleur réel

easy_admin_bundle:
    resource: '@EasyAdminBundle/Controller/'  # Assurez-vous qu'il n'y a pas de "/Admin" ici
    type: annotation
